This is a short description of how to move a commit from wrong branch to the right branch.
Use git cherry-pick to do that.
First copy the commit and move into the rignt branch and use git cherry-pick <commit>. It will move the commit into  to right branch.
